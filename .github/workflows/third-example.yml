name: Shell Script Workflow

on: push

jobs:
  shell_script_workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Step
        uses: actions/checkout@v5
     
      - name: list files
        run: ls -ltra

      - name: Script Execution step
        run: |
          chmod +x third-example.sh
          ./third-example.sh